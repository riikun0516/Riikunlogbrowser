# Generated by flutpak — https://github.com/o-murphy/flutpak
#
# This is your editable TEMPLATE manifest — commit it to git.
# Run "flutpak generate" on every build to produce the final output in generated/.
#
# SAFE TO EDIT   finish-args, build-commands, build-options, sdk-extensions, modules
# AUTO-INJECTED  tag, commit, modules, manifest.sources, pubspec-sources.json, and patches are set/appended by generate
app-id: jp.riikunjp.riikunlogbrowser
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: riikunlogbrowser
# Sandbox permissions — review and adjust for your app.
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio
  - --talk-name=org.freedesktop.portal.OpenURI
modules:
  - name: riikunlogbrowser
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        PUB_CACHE: /run/build/riikunlogbrowser/.pub-cache
    # Build steps — review and customise for your project.
    build-commands:
      - flutter pub get --offline
      - flutter build linux --release --no-pub
      - mkdir -p /app/riikunlogbrowser
      - cp -r "$BUNDLE_PATH"/. /app/riikunlogbrowser/
      - install -Dm755 flatpak/riikunlogbrowser-wrapper.sh /app/bin/riikunlogbrowser
      - install -Dm644 app/share/metainfo/jp.riikunjp.riikunlogbrowser.metainfo.xml /app/share/metainfo/jp.riikunjp.riikunlogbrowser.metainfo.xml
      - install -Dm644 app/share/applications/jp.riikunjp.riikunlogbrowser.desktop /app/share/applications/jp.riikunjp.riikunlogbrowser.desktop
      - install -Dm644 app/share/icons/hicolor/256x256/apps/jp.riikunjp.riikunlogbrowser.png /app/share/icons/hicolor/256x256/apps/jp.riikunjp.riikunlogbrowser.png
    sources:
      - type: git
        url: https://github.com/riikun0516/riikunlogbrowser.git
